This module extends `website_sale_checkout_skip_payment` to make the
"skip payment" behaviour the default for every user.

It is intended for private B2B portals where customers must never pay
online.
