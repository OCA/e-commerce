<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Page template for displaying Brands -->
    <template id="product_brands" name="Brands">
        <t t-call="website.layout">
            <t t-set="additional_title">Product Brands</t>
            <div id="wrap" t-attf-class="wrapper">
                <div class="container">
                    <div class="row">
                        <form action="/page/product_brands" class="form-horizontal mt32 col-lg-8" method="post" >
                            <div class="input-group" style="margin-left:60%;">
                                <div class="col-lg-6">
                                    <input type="text" name="search" class="form-control" placeholder="Search..." t-att-value="search"/>
                                </div>
                                <div class="col-lg-2">
                                    <button class="btn btn-primary">Search</button>
                                </div>
                            </div>
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        </form>
                    </div>
                    <br/>
                    <section>
                        <t t-if="brand_rec">
                            <div class="row">
                                <t t-foreach="brand_rec" t-as="o">
                                    <div class="col-lg-3 d-lg-inline-block">
                                        <a t-att-href="keep('/shop/brands',brand = o.id)">
                                            <div class="row text-center">
                                                <div class="col">
                                                    <img itemprop="image" width="200px" height="200px" class="img" t-attf-src="/website/image/product.brand/#{o.id}/logo#{'?max_width=300&amp;max_height=300'}"/>
                                                </div>
                                            </div>
                                            <div class="row text-center">
                                                <div class="col">
                                                    <t t-esc="o.name" />
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                </t>
                            </div>
                        </t>
                        <t t-if="not brand_rec">
                            <div class="text-center text-muted">
                                <h3 class="css_editable_display">No Brands Found.</h3>
                            </div>
                        </t>
                    </section>
                </div>
            </div>
        </t>
    </template>

    <template id="categories_recursive" inherit_id="website_sale.categories_recursive">
        <xpath expr="//span[@t-field='c.name']/.." position="attributes">
            <attribute name="t-att-href">keep('/shop/category/' + slug(c), category=0, brand=brand and brand.id or 0)</attribute>
        </xpath>
    </template>

    <template id="product_brand_sidebar" name="Brand Sidebar Item">
        <li class="nav-item">
            <a t-att-href="keep('/shop/brand/' + slug(b), category=category and category.id or 0, brand=0)" t-attf-class="nav-link#{' active' if b.id == int(brand or 0) else ''}">
                <span t-field="b.name"/>
            </a>
        </li>
    </template>

    <template id="products_brands" inherit_id="website_sale.products" active="False" customize_show="True" name="eCommerce Brands Sidebar">
        <xpath expr="//div[@id='products_grid_before']" position="inside">
            <ul class="nav nav-pills flex-column mt16">
                <li class="nav-item">
                    <a t-att-href="keep('/shop', category=category and category.id or 0, brand=0)" t-attf-class="nav-link#{'' if brand else ' active'} o_not_editable">All Brands</a>
                </li>
                <t t-foreach="brands" t-as="b">
                    <t t-call="website_sale_product_brand.product_brand_sidebar" />
                </t>
            </ul>
        </xpath>
        <xpath expr="//div[@id='products_grid_before']" position="attributes">
            <attribute name="class">col-lg-3 col-md-4 col-12</attribute>
        </xpath>
        <xpath expr="//div[@id='products_grid']" position="attributes">
            <attribute name="class">col-lg-9 col-md-8</attribute>
        </xpath>
    </template>

    <template id="option_collapse_categories_recursive" inherit_id="website_sale.option_collapse_categories_recursive">
        <xpath expr="//a[@t-field='categ.name']" position="attributes">
            <attribute name="t-att-href">keep('/shop/category/' + slug(categ), category=0, brand=brand and brand.id or 0)</attribute>
        </xpath>
    </template>


</odoo>
