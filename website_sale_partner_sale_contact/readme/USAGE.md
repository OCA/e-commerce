This module is an auto-installed bridge: it activates automatically when both
`website_sale` and `sale_partner_sale_contact` are installed. No configuration
is required.

When a contact places an order from the website (cart creation, login, or the
guest checkout flow), the order's customer is set to the parent company and the
contact is recorded in the **Sale Contact** field.
