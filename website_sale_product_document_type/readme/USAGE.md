This module auto-installs once `product_document_type` and `website_sale`
are both installed. No further configuration is needed: document types
configured on *Sales > Configuration > Products > Document Types* are used
to group the documents already published on the product page.
