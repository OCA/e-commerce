This module doesn't have any effect on the wishlist: adding a matrix-configured
product from the wishlist page falls back to the regular product configurator
instead of opening the matrix. A glue module such as
`website_sale_wishlist_product_matrix` would be needed to fix this.
